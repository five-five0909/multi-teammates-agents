{
  "name": "multi-teammates-agents",
  "version": "0.3.2",
  "description": "Coordinate cross-platform specialist agents with lightweight execution or durable Trellis-backed independent audit.",
  "author": {
    "name": "fifine"
  },
  "license": "MIT",
  "keywords": [
    "codex",
    "multi-agent",
    "orchestration",
    "expert-team"
  ],
  "skills": "./skills/",
  "mcpServers": {
    "expert-team": {
      "command": "node",
      "args": [
        "-e",
        "var fs=require('fs'); var path=require('path'); var pluginRoot=process.env.PLUGIN_ROOT || process.env.CLAUDE_PLUGIN_ROOT; if (!pluginRoot && fs.existsSync(path.join(process.cwd(),'scripts','expert_team_mcp_launcher.js'))) pluginRoot=process.cwd(); if (!pluginRoot) throw new Error('plugin root is unavailable'); require(path.join(pluginRoot,'scripts','expert_team_mcp_launcher.js'));"
      ]
    }
  },
  "interface": {
    "displayName": "Multi Teammates Agents",
    "shortDescription": "Run audited specialist agent teams",
    "longDescription": "Plan dependency-aware expert work in lightweight mode or use durable Trellis-backed execution, independent audit, resume, and human gates.",
    "developerName": "fifine",
    "category": "Productivity",
    "capabilities": [
      "Interactive",
      "Write"
    ],
    "defaultPrompt": [
      "Use $expert-team to coordinate this complex task."
    ]
  }
}
