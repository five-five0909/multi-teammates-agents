{
  "mcpServers": {
    "expert-team": {
      "command": "node",
      "args": [
        "-e",
        "var path=require('path'); var url=require('url'); var root=process.env.PLUGIN_ROOT||process.env.CLAUDE_PLUGIN_ROOT||process.cwd(); import(url.pathToFileURL(path.join(root,'bin','mta-plugin-mcp.js')).href);"
      ],
      "cwd": "."
    }
  }
}
